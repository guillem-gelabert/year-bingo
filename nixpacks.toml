[phases.setup]
# Nixpacks' Node provider typically only supports major versions.
# Pinning the nixpkgs archive ensures a Node version new enough for Nuxt 4.x
# (requires ^20.19.0 || >=22.12.0).
nixpkgsArchive = "51ad838b03a05b1de6f9f2a0fffecee64a9788ee"


